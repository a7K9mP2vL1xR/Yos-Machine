# Yos's Machine

This is type of computer with value range 0 to unbounded and there memory and input device and output device, if you command/send the value to the memory and then you command/accept using input device or others (it decide by progam or creature/human via progam or simply decice using input) it will show output that are determined by the size of value you send; and can Increment and decrement the memory and or other methods; so the memory is empty for ready other value, and also simply; single value per memory or it just like register, and also memory have no fixed maximum size and also expandable and if you send value bigger than the memory capacity it just the biggest value and also if you send more value if it's the memory full occupied it can't, it expandable use distribution or 3d builder; and the engine for the expandable are progammable, and its can conditional and loop because of the data that stored in storage of output device tell what the meaning of that value which some data can mean go to the device that is controlling memory, so it can do 3 things of input is capacity and value and accept.

There Maybe Similar like That:

- Turing machine

- Register machine / Minsky counter machine

- Two-stack machine

- Little Man Computer

- Pushdown automaton

# Yosh-Machine
Yosh's Machine (no memory?)

This is type of computer with value range 0 to unbounded and there input device and output device, if you command/send the value and then command/accept using input device or others (it decide by progam) it simply show output that from what value you send.

Yosh's Machine

This is type of computer with value range 0 to unbounded and there memory and input device and output device, if you command/send the value to the memory and then you command/accept using input device or others (it decide by creature/human/progam (so not autonomous ok)) it will show output that are determined by the size of value you send; and can Increment and decrement the memory and or other methods; so the memory is empty for ready other value, and also simply; simgle value per memory or it just like register, and also memory have no fixed maximum size and also expandable and if you send value bigger than the memory capacity it just the biggest value and also if you send more value if it's the memory full occupied it can't, it expandable use distribution; and the engine for the expandable are progammable, and its turing comple because of the data that stored in storage of output device tell what the meaning of it that value which some data can mean go to the that device is controlling temporary memory so can loop and conditional.
